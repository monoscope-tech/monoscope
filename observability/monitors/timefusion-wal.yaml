active: true
alert_recovery_threshold: null
alert_threshold: 6.442450944e10
check_interval_mins: 15
email_all: true
emails: []
message: WAL dir above 60GB — steady state should be ~retention+20min of ingest (~30GB
  at 6MB/s). Either the mtime reaper stopped, walrus leaked across rapid restarts,
  or ingest spiked. Check timefusion.wal.gc_deleted_files and the WAL GC log lines.
notify_after_mins: null
query: metrics | where metric_name == "timefusion.wal.disk_bytes" | summarize max(value)
severity: warning
slack_channels: []
stop_after_count: null
subject: TimeFusion WAL directory growing beyond expected steady state
teams: []
threshold_sustained_for_mins: 0
time_window_mins: 30
title: 'TF warn: WAL disk usage > 60GB'
trigger_less_than: false
visualization_type: timeseries
warning_recovery_threshold: null
warning_threshold: null

